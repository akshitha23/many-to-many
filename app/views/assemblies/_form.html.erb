<%= form_for(@assembly) do |f| %>
  <% if @assembly.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assembly.errors.count, "error") %> prohibited this assembly from being saved:</h2>

      <ul>
      <% @assembly.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div><br><br>
  <%=f.fields_for :parts do |p|%>
    <%= p.label :part_number %><br>
    <%= p.text_field :part_number %><br><br>
  <% end%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
